[project]
name = "project-a"
version = "0.1.0"
description = "Tiny Python service placeholder for SkillHost tests"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
